export default function E2W9ResoLearningChecklist() {
  return (
    <div className="page-content">
      <h1>Week 9: Learning Checklist</h1>
      <ul>
        <li>[ ] I understand the difference between raw SQL and an ORM.</li>
        <li>[ ] I know how to configure a connection string in <code>appsettings.json</code>.</li>
        <li>[ ] I can create a <code>DbContext</code> and add <code>DbSet</code> properties.</li>
        <li>[ ] I can run <code>add-migration</code> and <code>update-database</code>.</li>
        <li>[ ] I can write a basic LINQ <code>.Where()</code> query.</li>
        <li>[ ] I know to call <code>.ToList()</code> after <code>.Where()</code>, not before.</li>
      </ul>
    </div>
  )
}
