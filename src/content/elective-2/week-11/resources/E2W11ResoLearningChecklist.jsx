export default function E2W11ResoLearningChecklist() {
  return (
    <div className="page-content">
      <h1>Week 11: Learning Checklist</h1>
      <ul>
        <li>[ ] I know why we use <code>IHttpClientFactory</code> instead of <code>new HttpClient()</code>.</li>
        <li>[ ] I can write an <code>async Task&lt;IActionResult&gt;</code> controller method.</li>
        <li>[ ] I can use <code>JsonSerializer.Deserialize</code> to convert JSON to a C# object.</li>
        <li>[ ] I understand the purpose of a DTO class.</li>
        <li>[ ] I can handle API errors with try/catch.</li>
      </ul>
    </div>
  )
}
